[package]
name    = "project-firmware-sim"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
heapless = "0.8.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
