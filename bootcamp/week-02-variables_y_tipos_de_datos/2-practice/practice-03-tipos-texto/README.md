# 📝 Práctica 03: Tipos de Texto

## 🎯 Objetivo

Practicar los tipos de texto en Rust: `char`, `&str` y `String`.

## 📋 Instrucciones

### Parte 1: Caracteres (char)

1. Declara caracteres ASCII: letras, números, símbolos
2. Declara caracteres Unicode: emojis, acentos
3. Usa métodos como `is_alphabetic()`, `is_numeric()`

### Parte 2: String Slices (&str)

1. Declara string literals
2. Entiende que son referencias inmutables
3. Usa métodos como `len()`, `is_empty()`

### Parte 3: Strings Owned (String)

1. Crea String con `String::from()`
2. Crea String con `.to_string()`
3. Modifica el String (push, push_str)

### Parte 4: Booleanos

1. Declara variables true/false
2. Usa operadores lógicos: &&, ||, !
3. Compara valores

## ✅ Criterios de Éxito

- [ ] Usar correctamente char con comillas simples
- [ ] Diferenciar &str de String
- [ ] Realizar operaciones con booleanos

## 🧪 Ejecutar

```bash
cargo run
cargo test
```
