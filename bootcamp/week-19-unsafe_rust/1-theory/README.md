# Semana 19 — Teoría: `unsafe` Rust y Raw Pointers

> ⚠️ **TODO**: Completar con teoría completa (~180 líneas).
> Ver [instrucciones del bootcamp](../../../.github/copilot-instructions.md) para el formato esperado.

## Secciones pendientes

1. ¿Qué es `unsafe` en Rust? Las 5 superpoderes
2. Raw pointers: `*const T` y `*mut T`
3. Funciones y bloques `unsafe`
4. `unsafe` traits: `Send` y `Sync`
5. `std::mem`: `transmute`, `size_of`, `align_of`, `forget`, `replace`
6. Invariantes de seguridad y documentación con `// SAFETY:`
7. Errores comunes y comportamiento indefinido (UB)
8. Herramientas: Miri, AddressSanitizer
